{
  "hooks": [
    {
      "event": "PreCompact",
      "description": "Update handoff before compact",
      "command": "wscript //nologo \"${CLAUDE_PLUGIN_ROOT}/scripts/precompact-hidden.vbs\""
    },
    {
      "event": "Stop",
      "description": "Log decisions after each response",
      "command": "wscript //nologo \"${CLAUDE_PLUGIN_ROOT}/scripts/stop-hidden.vbs\""
    }
  ]
}
