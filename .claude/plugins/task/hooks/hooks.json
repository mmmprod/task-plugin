{
  "hooks": [
    {
      "event": "PreCompact",
      "description": "Update handoff before compact",
      "command": "\"${CLAUDE_PLUGIN_ROOT}/scripts/precompact-launcher.bat\""
    },
    {
      "event": "Stop",
      "description": "Log decisions after each response",
      "command": "\"${CLAUDE_PLUGIN_ROOT}/scripts/stop-launcher.bat\""
    }
  ]
}
